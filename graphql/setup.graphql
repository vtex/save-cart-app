type ChangeSetupResponse @context(scope: "private") {
	success: Boolean
	messages: [String]
}

type GetSetupConfigResponse @context(scope: "private") {
    adminSetup: AdminSetupResponse
}

type AdminSetupResponse @context(scope: "private") {
    cartName: String
    cartLifeSpan: Int
}
