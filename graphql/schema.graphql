type Query {
  changeAdminSetup (
    cartName: String,
    cartLifeSpan: Int,
  ): ChangeSetupResponse
  getSetupConfig : GetSetupConfigResponse
}

type Mutation {
  saveCart(cart: Cart) : Boolean!
  getCarts(email: String) : [CartResponse]
  removeCart(id: String) : Boolean!
}
