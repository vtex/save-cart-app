type Query {
  currentTime: String
}

type Mutation {
  saveCart(cart: Cart) : String @cacheControl(scope: PRIVATE)
  getCarts(email: String) : [CartResponse] @cacheControl(scope: PRIVATE)
  removeCart(id: String) : Boolean! @cacheControl(scope: PRIVATE)
}
